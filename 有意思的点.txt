
1、训练数据的组织形式是[cls]song_name[sep]lyric[sep]lyric[sep]···
2、提供了一个模板，可以自定义一些歌词，不自定义的地方，模型会自动生成。
3、可以给定生成的长度，模型会尝试n次去生成指定长度的句子，如果超出n次的尝试则会自由生成。
4、歌名始终贯穿了整个训练，每次生成句子，在句首都会有歌名的attention出现，用于点题。
5、当句子生成失效的时候，会自适应的调整temperature，让模型的泛化性能提高。